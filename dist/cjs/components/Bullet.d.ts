import { FC } from "react";
import { Color } from "../themes";
interface Props {
    /**
     * Component children
     */
    children?: any;
    /**
     * Component children
     */
    background?: Color;
}
export declare const Bullet: FC<Props>;
export {};
